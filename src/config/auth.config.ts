export const AUTH_CONFIG = {
  // User info refresh interval (milliseconds)
  USER_INFO_REFRESH_INTERVAL: 5 * 60 * 1000, // 5 minutes
  
  // Session timeout (minutes)
  SESSION_TIMEOUT_MINUTES: 30,
  
  // Maximum retry count
  MAX_REFRESH_RETRIES: 3,
  
  // Retry delay (milliseconds)
  RETRY_DELAY_MS: 1000
};
